// Checagem de tipos incluindo os testes, que o tsconfig.json de build exclui.
// Uso: `npm run typecheck`.
{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true
  },
  "include": ["src/**/*", "prisma.config.ts"],
  "exclude": ["node_modules", "build"]
}
